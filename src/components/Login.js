import React from 'react'

function Login({handleClick}) {
    return (
        <div>
            6666
            <button onClick={handleClick} >7777</button>
        </div>
    )
}

export default Login
